
// Built by AutumnGrey, with Claude (claude.ai) along for the ride as a pair
// programmer: writing, reviewing and testing a good deal of what follows.
// Every design decision, and every correction of the ones that were wrong,
// was Autumn's.
//
// Bumped on every publish. Written in one place, shown in the footer and
// stamped into every export, so a bug report can be tied to a build.
const APP_VERSION="v0.14.2";
// ISO date of that publish, shown in the footer exactly as written. ISO reads
// the same everywhere, so no locale formatting is applied to it.
const APP_UPDATED="2026-09-23";
